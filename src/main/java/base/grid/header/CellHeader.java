package base.grid.header;

import base.grid.BaseCell;

public class CellHeader extends BaseCell {

	public CellHeader(String selector, int row, int column, TableCellHeader headerElement) {
		super(selector, row, column);
	}
}
