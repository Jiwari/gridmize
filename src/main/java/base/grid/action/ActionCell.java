package base.grid.action;

import base.grid.BaseCell;

public class ActionCell extends BaseCell {
	public ActionCell(String selector, String actionsSelector, int row, int column) {
		super(selector, actionsSelector, row, column);
	}
}
